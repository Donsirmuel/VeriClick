# Logs (runtime output, never commit)
*.log
*.log.*

# Local environment (never commit)
.env
.env.*
!.env.example

# TLS certificates (secret, never commit)
certs/**/*.crt
certs/**/*.key
certs/**/*.pem
